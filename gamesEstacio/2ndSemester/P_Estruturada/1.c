#include<stdio.h>

int main()
{
    printf( sizeof(int) );
}

/* The sizeof function returns the number of byte a variable has, it can also show the number of bytes a data type has. This function was something I learnt at the College.*/